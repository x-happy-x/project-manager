# Changelog

## [1.0.0] - 2025-01-XX

### Реализованные возможности

#### Основная функциональность
- ✅ Управление проектами через YAML конфиги
- ✅ Регистрация проектов (`pm add`, `pm rm`, `pm ls`)
- ✅ Выполнение пользовательских команд
- ✅ RAW режим для произвольных команд в директории проекта
- ✅ Поддержка Windows и Linux (WSL)

#### Функции (func)
- ✅ Определение переиспользуемых функций
- ✅ Параметры с required/default значениями
- ✅ Multiline script (array или string)
- ✅ Вызов функций через `_{func(param=value)}`
- ✅ Глобальные функции через `_{global.func()}`

#### Шаблоны
- ✅ Параметры команд: `@{args}`, `@{param}`
- ✅ Environment переменные: `${VAR}`
- ✅ Конфиг проекта: `#{info.name}`, `#{info.root}`
- ✅ Глобальный конфиг: `#{global.vars.x}`
- ✅ Рекурсивная подстановка в функциях

#### Docker Compose
- ✅ Интеграция с docker-compose
- ✅ Группировка сервисов (groups)
- ✅ Встроенная команда `:up` с поддержкой `@group`
- ✅ Возможность миксовать группы и отдельные сервисы

#### Рендеринг
- ✅ Bash renderer (Linux/WSL)
- ✅ PowerShell renderer (Windows)
- ✅ Плагинная система для внешних рендереров
- ✅ Корректная работа с pushd/popd
- ✅ Экранирование спецсимволов

#### Конфигурация
- ✅ Реестр проектов в `~/.config/pm/registry.yml`
- ✅ Глобальный конфиг в `~/.config/pm/global.yml`
- ✅ Переменная окружения `PM_CONFIGS` для кастомизации
- ✅ Переменная `PM_DIALECT` для выбора shell
- ✅ Переменная `PM_PLUGIN_DIR` для плагинов

#### Тестирование
- ✅ Unit тесты для всех модулей
- ✅ E2E тесты полного цикла (конфиг → скрипт)
- ✅ Покрытие кода >60% (критичные модули >90%)
- ✅ Тесты для bash и PowerShell рендеринга
- ✅ Тесты docker compose групп

#### Документация
- ✅ README.md с основными примерами
- ✅ EXAMPLES.md с реальными сценариями
- ✅ ARCHITECTURE.md с описанием устройства
- ✅ Примеры конфигов в samples/

### Технические детали

#### Структура проекта
```
project-manager/
├── cmd/
│   └── pm-bin/          # Главный бинарник
├── internal/
│   ├── config/          # Конфиги и реестр
│   ├── dsl/             # Парсинг командной строки
│   ├── templ/           # Шаблонизатор
│   ├── plan/            # Абстрактный план
│   ├── render/          # Shell рендереры
│   ├── docker/          # Docker compose
│   └── e2e/             # Интеграционные тесты
├── samples/             # Примеры конфигов
├── pm.sh                # Bash wrapper
├── pm.ps1               # PowerShell wrapper
└── README.md
```

#### Зависимости
- Go 1.23+
- `gopkg.in/yaml.v3` - парсинг YAML
- `github.com/google/shlex` - парсинг shell аргументов (в будущем)

#### Архитектурные решения

**Двухфазная архитектура**:
1. pm-bin строит план команд
2. Wrapper выполняет через eval

Причины:
- Изоляция процессов
- Корректная работа cd/pushd/popd
- Environment переменные
- Pipes и redirects

**Plan-based approach**:
- Абстрактный план операций
- Независимость от целевого shell
- Простота тестирования

**Template system**:
- Regex-based подстановка
- Порядок: env → params → config → functions
- Рекурсивный рендеринг функций

### Примеры использования

#### Простой проект
```yaml
info:
  name: myapp
  root: ~/repos/myapp

commands:
  build:
    cmd: "npm run build"

  test:
    cmd: "npm test @{args}"
```

```bash
pm add ~/repos/myapp/.pm.meta.yml
pm myapp :build
pm myapp :test --watch
```

#### С функциями
```yaml
func:
  use-java:
    params:
      version:
        default: "21"
    script: "sdk use java @{version}"

commands:
  build:
    cmd:
      - "_{use-java()}"
      - "./gradlew build"
```

#### С Docker
```yaml
docker:
  compose_file: docker-compose.yml
  groups:
    db: [postgres, redis]
    app: [api, worker]

commands:
  dev:
    cmd: "npm run dev"
```

```bash
pm myapp :up @db
pm myapp :dev
```

### Протестированные сценарии

#### 1. Базовые операции
- [x] Добавление/удаление/список проектов
- [x] Выполнение команд по имени
- [x] RAW команды
- [x] Множественные команды в одном вызове

#### 2. Функции
- [x] Простые функции
- [x] Функции с параметрами
- [x] Функции с defaults
- [x] Обязательные параметры
- [x] Multiline функции
- [x] Глобальные функции

#### 3. Шаблоны
- [x] @{args} подстановка
- [x] @{custom_param}
- [x] ${ENV} переменные
- [x] #{info.*} из конфига
- [x] #{global.*} из глобального конфига

#### 4. Docker
- [x] :up без аргументов
- [x] :up с сервисами
- [x] :up с @groups
- [x] Множественные @groups
- [x] Миксование @groups и сервисов

#### 5. Рендеринг
- [x] Bash для Linux/WSL
- [x] PowerShell для Windows
- [x] Экранирование спецсимволов
- [x] Pushd/popd генерация

#### 6. Edge cases
- [x] Несуществующая команда
- [x] Функция без обязательного параметра
- [x] Несуществующая группа Docker
- [x] Пустые аргументы
- [x] Спецсимволы в путях

### Известные ограничения

#### Не реализовано (но можно добавить)
- [ ] Вложенные вызовы функций `_{f1(x=_{f2()})}`
- [ ] Условная логика (if/else)
- [ ] Циклы
- [ ] Dependency graph между командами
- [ ] Pre/post hooks
- [ ] Interactive mode (TUI)
- [ ] Shell completion
- [ ] Watch mode

#### Ограничения дизайна
- Raw и named режимы взаимоисключающие
- Функции не могут возвращать значения (только генерят команды)
- Нет встроенных переменных (кроме @{args})
- Относительные пути #{.} сложны в использовании

### Совместимость

#### Операционные системы
- ✅ Linux (bash)
- ✅ WSL (bash)
- ✅ Windows (PowerShell)
- ⚠️  macOS (не тестировалось, должно работать как Linux)

#### Shell
- ✅ bash 4+
- ✅ PowerShell 5+
- ⚠️  zsh (alias для bash, должно работать)
- ❌ fish (нужен плагин)
- ❌ cmd.exe (не поддерживается)

### Производительность

#### Бенчмарки (Windows)
- Холодный старт: ~20-50ms
- pm ls: ~10-20ms
- pm :command: ~20-40ms
- pm raw: ~15-30ms

#### Оптимизации
- Minimal I/O
- No external deps (кроме yaml)
- Lazy loading глобального конфига
- Маленький бинарник (<2MB)

### Migration Guide

Если у вас уже был свой project manager:

#### 1. Конвертация конфигов
```bash
# Старый формат
{
  "name": "myapp",
  "commands": {
    "build": "npm run build"
  }
}

# Новый формат
info:
  name: myapp
  root: ~/repos/myapp
commands:
  build:
    cmd: "npm run build"
```

#### 2. Миграция функций
Если были переиспользуемые скрипты - вынести в `func:`

#### 3. Docker compose
Если были алиасы для групп сервисов - использовать `docker.groups`

### Благодарности

Спасибо Claude Code за помощь в разработке! 🚀
